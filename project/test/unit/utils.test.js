// include tests for functions like meal macro calculation and list price estimate
const { expect } = require('chai');

describe('Basic Test', () => {
  it('should add numbers correctly', () => {
    expect(2 + 2).to.equal(4);
  });
});