<div class="meal" 
  data-id={{id}}
  data-name="{{name}}"
  data-servings={{servings}}
  data-mealType="{{#each mealType}}{{this}} {{/each}}"
  data-categoryTags="{{#each categoryTags}}{{this}} {{/each}}"
  data-ingredients="{{#each categoryTags}}{{this}} {{/each}}"
  data-description="{{description}}"
  data-image="{{image_link}}"
>
    <img src="{{image_link}}" alt="Picture of {{name}}">
    <p><a href="/meals/{{id}}">{{name}}</a></p>
    <button class="add-ingredients-button">Add to List</button>
</div>