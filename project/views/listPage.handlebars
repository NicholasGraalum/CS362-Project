<main>
    <div class="ingredients-bar">
      <h2>Ingredients</h2>
    </div>
    <div class="ingredients-grid" id="all-ingredients-grid">
        {{#each ingredients}}
        {{> ingredient}}
        {{/each}}
    </div>
</main>